# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Claude Code personal (per-developer) settings
.claude/settings.local.json

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# Watchman
.watchman-cookie-*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

example

# generated native folders
/ios
/android

# secrets — never commit signing keys / API keys
*.p8
*.p12
*.jks
*.keystore
*.mobileprovision

# ASC API key — CI가 시크릿(ASC_API_KEY_P8)에서 복원; 로컬 제출 시 ~/credentials에서 복사
asc-api-key.p8

# 로컬 EAS 빌드 산출물
*.apk
*.aab
*.ipa
rougether-preview.apk.zip
*.apk.zip

# Play 제출 서비스 계정 키 (콘솔에서 발급, 커밋 금지)
play-service-account.json

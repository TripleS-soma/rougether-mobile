name: Deploy web (PWA)

on:
  push:
    branches: [main]
    paths:
      [
        'src/**',
        'public/**',
        'assets/**',
        'app.json',
        'package.json',
        'package-lock.json',
        '.github/workflows/web.yml',
      ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: web-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # 카카오 웹 로그인의 REST API 키(비밀 아님 — 번들에 인라인됨). repo variable KAKAO_REST_API_KEY.
      - run: npx expo export --platform web --output-dir dist
        env:
          EXPO_PUBLIC_KAKAO_REST_API_KEY: ${{ vars.KAKAO_REST_API_KEY }}
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4

Original prompt: 미니게임에서 캐릭터는 우리 캐릭터 고양이로 잡으면 좋을 것 같아. 디자인도 기존 루게더 스타일로. 다른 에이전트도 깨워서 병렬적으로 미니게임 다른 종류 만들어.

2026-09-12

- `cat-stairs` 고양이 계단 오르기를 추가했다. 다음 계단 방향에 맞는 왼쪽/오른쪽 버튼이나 키보드 방향키를 누른다. 방향 실수 또는 시간 초과로 종료한다.
- 기존 `cat-2.webp`와 `cat-3.webp`의 원본 data URI를 가져온다. 새로운 캐릭터를 생성하거나 기존 외형을 수정하지 않았다. 루게더의 크림색/녹색 토큰, 원본 고양이, 둥근 계단과 버튼으로 구성했다.
- 최초 제한 시간은 계단당 3초이며 성공 5회마다 0.1초씩 줄고 최소 0.75초를 유지한다. 최대 2분이며 같은 입력을 반복하는 키 홀드는 허용하지 않는다.
- 버전 1 리플레이는 `{ticks, actions:[{tick,direction:'LEFT'|'RIGHT'}]}`이다. 60Hz, 최대 7200틱/1200입력, 입력 사이 최소 6틱을 적용한다. 서버가 seed와 입력으로 점수를 계산한다. 시간 초과 틱의 입력은 기록하지 않는다.
- Java 서버와 공유하는 8개 fixture에 무입력, 오답, 정답 후 시간 초과, 경계 방향 전환, 2분 완주, 최소 타이머, 최대 1200입력을 포함했다. 서버 에이전트도 8개 fixture를 실제 Java 검증기로 재현했다.
- 3개 Jest suite의 39개 테스트가 통과했다. 엔진, 메시지 검증, HTML 경계, 네이티브 WebView 생명주기와 완료 콜백 중복 방지를 검증했다.
- develop-web-game 원본 Playwright 클라이언트로 시작과 계단 입력을 실행하고 스크린샷을 직접 검토했다. 추가 브라우저 검증에서 터치 8계단, 잘못된 방향, 시간 초과, 시작/정지/재개 키보드, 비활성화, 잘못된 메시지 채널, 종료 정리 등 10개 흐름이 통과했다. 브라우저 실행 오류는 없었다.
- 랭킹 문서에는 `advanceTime`이 노출되지 않는다. 개발 연습 모드에서만 수동 시간 이동을 허용한다. 랭킹 문서의 실제 프레임 진행도 확인했다.
- 캡처와 보고서는 `output/minigame-qa/stairs/`에 있다. 실제 네이티브 실기기 확인은 수행하지 않았다. 게임 목록/화면/랭킹 통합은 별도 에이전트가 담당한다.

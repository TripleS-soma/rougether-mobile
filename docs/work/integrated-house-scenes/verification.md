# 집 통합 장면 검증 기록

검증일: 2026-09-11. 기준 dev: `f5b956de32a56d3fce6f1ed7a1d095fab075a4c9`.

## 자동 검사

- `npm run typecheck`: 통과.
- `npm run lint`: 오류 0개. 기존 `app-shell.tsx:294`의 `fromGachaRef` 의존성 경고 1개가 남아 있습니다.
- `npm run format:check`: 통과.
- `npm test -- --runInBand --watchman=false --forceExit`: 264 suites, 2,096 tests 통과.
- `git diff --check`: 통과.

전체 Jest 실행은 테스트 종료 후 기존 비동기 handle이 남아 `--forceExit`를 사용했습니다. 별도로 신규 통합 장면 테스트의 `--detectOpenHandles` 실행은 정상 종료했습니다. Watchman은 로컬 sandbox의 상태 파일 권한 오류를 피하도록 비활성화했습니다.

새 테스트는 네 테마 × 2/4/6 정원의 정확한 12개 조합, 원본 좌표 변환, 화면 폭에 따른 방 경계 보존, 정원·테마 미지원과 이미지 실패 fallback, 그림·방의 렌더 순서, 좌석 방문, 미리보기와 갤러리를 검사합니다. 기존 방·가구·제스처 테스트도 전체 검사에 포함됩니다.

## 실제 RN Web 확인

고정 fixture와 공개 에셋으로 확인했으며 개인 회원 API 데이터는 사용하지 않았습니다.

- 390×844에서 12개 조합을 모두 열었습니다. 방 48개가 화면 영역 안에 있고 별도 배경이 중복 렌더되지 않았습니다.
- 각 장면의 그림, 벽, 바닥, 가구, 캐릭터를 포함한 이미지 252개가 로드되었습니다.
- 2인 좌석 순서는 `1, 2`, 4인은 위부터 `3, 4 / 1, 2`, 6인은 `5, 6 / 3, 4 / 1, 2`였습니다.
- 실제 집 화면에서 2번 멤버를 눌러 해당 membershipId의 방문 콜백을 확인했습니다.
- 별도 검토자가 mushroom/cloud 6인(390×844), coral 4인(375×667), night 6인(320×568)의 자연스러운 접합과 방 경계·순서를 확인했습니다.
- 브라우저 도구가 반환한 inline 캡처로 확인했습니다. 별도의 스크린샷 파일은 저장하지 않았습니다.

개발 갤러리: `/dev?entry=IntegratedHouseScenes`.

전체 화면 예: `/dev?entry=IntegratedHouseViewport&viewport=1&theme=cloud-balloon&capacity=6`.

갤러리에서 실제 집/탐색 미리보기, 빈자리, 레거시 프레임으로 전환할 수 있습니다. 다크 모드의 색 처리와 핀치 줌·롱프레스 자리 교환은 네이티브 실기기에서 추가 확인이 필요합니다. 웹 확인을 실기기 확인으로 간주하지 않습니다.

## 에셋과 네이티브 지문

원본 manifest와 최종 번들에 12장·48개 방이 있으며 WebP 합계는 2,175,948 bytes입니다. 생성 패키지 검사는 SHA-256, 캔버스 범위, 같은 집의 동일 방 크기, 정확한 5:6 비율을 통과했습니다.

같은 lockfile로 의존성을 설치한 기준 dev와 변경본의 Expo fingerprint가 일치했습니다.

| 플랫폼  | 기준 dev와 변경본 공통 fingerprint         |
| ------- | ------------------------------------------ |
| iOS     | `8441e2f0e567fbd486e3a699fd20ec36c4a82ca2` |
| Android | `6cf5b8fd403edccd377dc0ee8cfc2b20d132ebb7` |

앱 설정과 네이티브 모듈은 변경하지 않았습니다. 이번 검증은 로컬 작업이며 merge, OTA, CDN 업로드, 네이티브 빌드, 스토어 제출은 수행하지 않았습니다.

독립 코드 리뷰의 런타임 P1 지적은 없었으며, 지적된 테스트 파일 세 개의 포맷을 수정한 뒤 전체 포맷 검사를 통과했습니다. 이슈 #1296은 생성했지만 프로젝트 보드 등록은 토큰의 `read:project` 권한 부족으로 완료하지 못했습니다.

## 실기기 좌우 잘림 후속 수정

iPhone 15의 393×852 화면에서 2인 장면의 지붕·외벽이 잘렸습니다. 기존 계산이 화면 높이를 채우려고 원본을 479.50px 폭으로 확대하고 -43.25px 이동했습니다. 방 좌표만 보호하는 경계 검사로는 원본 바깥쪽의 지붕과 정원을 보호하지 못했습니다.

자동 확대를 제거해 원본 전체 폭을 393px에 맞춥니다. 그림과 방은 같은 비율을 유지하며, 남는 하단은 장면의 바닥색과 24px 페이드로 연결합니다. 핀치 카메라와 스크롤, 저장된 가구 좌표는 변경하지 않았습니다.

회귀 검사는 실제 `HouseScreen`의 12개 장면에 iPhone 크기와 화면 높이 변경을 적용하여 전체 원본 폭, 음수 이동 없음, 방 좌표 정합성, 바닥색과 실패 시 fallback을 확인합니다. 기존의 방 경계만 계산하던 단위 검사를 대체했습니다.

- production 호환 소스: 254 suites / 2,009 tests 통과.
- dev 소스: 264 suites / 2,095 tests 통과.
- 양쪽 typecheck·lint·format·diff 검사 통과. lint의 기존 hook 경고와 Jest 종료 옵션은 위 기록과 같습니다.
- 수정 후 iOS fingerprint: `8441e2f0e567fbd486e3a699fd20ec36c4a82ca2`로 동일합니다.
